{
  "Meta": {
    "Env": "${META_ENV}",
    "AppName": "${META_APP_NAME}",
    "ApiKey": "${META_API_KEY}",
    "LogPath": "${META_LOG_PATH}",
    "LogBaseLevel": "${META_LOG_BASE_LEVEL}",
    "LogMicrosoftLevel": "${META_LOG_MICROSOFT_LEVEL}"
  },
  "SQL": {
    "HOST": "${SQL_HOST}",
    "PORT": "${SQL_PORT}",
    "USER": "${SQL_USER}",
    "PASSWORD": "${SQL_PASSWORD}",
    "DATABASE": "${SQL_DATABASE}"
  },
  "ASPNETCORE_ENVIRONMENT": "Development",
  "ENGINE": "SQLSERVER"
}